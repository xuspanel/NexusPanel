<% const body = `
<section class="n-section n-docs">
<h1>Firewall Rules</h1>
<p>Manage iptables rules through an intuitive chain-based interface. Add and delete rules by chain and line number. Save rules persistently. Supports input, output, and forward chains.</p>
<h2>Getting Started</h2>
<p>Access this feature from the sidebar navigation after logging into NexusPanel. All features are available to admin users.</p>
<h2>Configuration</h2>
<p>Most features work automatically with default system configurations. For advanced setup, refer to the specific service configuration files referenced in the feature documentation.</p>
<h2>Keyboard Shortcuts</h2>
<p>Where available, keyboard shortcuts are listed in the feature header. Common shortcuts include Ctrl+S (save), Escape (close), and Ctrl+A (select all).</p>
</section>
`; %>
<%- include('../layout', { body: body }) %>
